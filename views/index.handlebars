<h2>Add a burger</h2>
<form id="create-form">

  <div class="form-group">
    <label class="burger" for="ca">Burger Name:</label>
    <input type="text" id="burgr" name="name">
  </div>

  {{!-- <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input type="text" name="devoured" value="1" id="devoured" checked > devoured!<br>
      </div> --}}

  <button type="submit">Add burger</button>
</form>


<h1>Burgers!</h1>

<h2>Burgers that are not devoured!</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}
  {{> burgers/burger-block devoured=true}}
  {{/unless}}
  {{/each}}
</ul>

<h2>RIP Burgers</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  {{> burgers/burger-block devoured=false}}
  {{/if}}
  {{/each}}
</ul>

{{!-- <h2>Add a burger</h2>
<form id="create-form">

  <div class="form-group">
    <label class="burger" for="ca">Burger Name:</label>
    <input type="text" id="burgr" name="name">
  </div> --}}

  <div class="form-group">
    <label for="devoured">devoured?</label><br>
    <input id ="radio" name="devoured" value="1" id="devoured" checked > devoured!<br>
      </div>

  {{!-- <button type="submit">Add burger</button>
</form> --}}
